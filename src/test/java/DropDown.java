import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

public class DropDown {
   /*
    Select something = new Select(driver.getDriver);
    List<WebElement> web = something.getOption();
    List<String > actualStr = new ArrayList<>();
    for(WebElement each : web){
     actualStr.add(each.getText());
    Assert.assertEquals(expected,actualStr);

} */
}
